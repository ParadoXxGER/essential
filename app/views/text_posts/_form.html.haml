= form_for @text_post do |f|
  - if @text_post.errors.any?
    #error_explanation
      %h2= "#{pluralize(@text_post.errors.count, "error")} prohibited this text_post from being saved:"
      %ul
        - @text_post.errors.full_messages.each do |message|
          %li= message

  .actions
    = f.submit 'Save'
