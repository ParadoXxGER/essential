= form_for @file_post do |f|
  - if @file_post.errors.any?
    #error_explanation
      %h2= "#{pluralize(@file_post.errors.count, "error")} prohibited this file_post from being saved:"
      %ul
        - @file_post.errors.full_messages.each do |message|
          %li= message

  .actions
    = f.submit 'Save'
