= form_for @photo_album_post do |f|
  - if @photo_album_post.errors.any?
    #error_explanation
      %h2= "#{pluralize(@photo_album_post.errors.count, "error")} prohibited this photo_album_post from being saved:"
      %ul
        - @photo_album_post.errors.full_messages.each do |message|
          %li= message

  .actions
    = f.submit 'Save'
